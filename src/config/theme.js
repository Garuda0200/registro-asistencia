// src/config/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',  // Puedes elegir el color principal que desees
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;
